let initState = {

}
export default function Reducer(state=initState, action){
    switch (action.type) {
        // case value:
        //     return state
        default:
            return state
    }
}